# REESCREVA O ARQUIVO COMPLETO: nginx/default.conf

server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # NOVA REGRA PARA SERVIR AS VARIÁVEIS DE AMBIENTE
    # A gente usa '=' para uma correspondência exata, é mais performático.
    location = /api/env {
        # Este é um caminho especial dentro do container do Easypanel que contém as variáveis.
        alias /etc/easypanel/variables.js;
        
        # Define o tipo de conteúdo para que o navegador entenda que é JavaScript/JSON.
        default_type application/javascript;
        
        # Instrução para o navegador NUNCA guardar esse arquivo em cache. Essencial para segurança.
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Regra para o SPA: se não achar o arquivo, manda pro index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy reverso para o N8N (agora usando a rede interna do Docker)
    location /api/ {
        # 🚨 GARANTA QUE ESTE NOME ESTÁ CORRETO 🚨
        # nome-do-projeto-n8n + nome-do-serviço-n8n
        proxy_pass http://n8n-n8n-editor:5678/webhook/;

        # Configurações essenciais do proxy
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}